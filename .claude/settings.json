{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'if echo \"$INPUT\" | jq -r .tool_input.command | grep -q \"git commit\"; then npx tsc --noEmit; fi'",
            "statusMessage": "Type-checking TypeScript..."
          }
        ]
      }
    ]
  }
}
