# Database Configuration (Oracle)
# Default values work with docker-compose setup
DB_USER=MELOSYS
DB_PASSWORD=melosys
DB_CONNECT_STRING=localhost:1521/freepdb1

# For Intel Mac or GitHub Actions, use:
# DB_CONNECT_STRING=localhost:1521/XEPDB1

# Base URL (optional, defaults to localhost:3000)
BASE_URL=http://localhost:3000

# Playwright Options (optional)
# PWDEBUG=1                    # Enable Playwright debug mode
# HEADLESS=false               # Run browser in headed mode
